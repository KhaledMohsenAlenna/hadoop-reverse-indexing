# hadoop-reverse-indexing
Built a Distributed Reverse Indexing engine using Hadoop's MapReduce and HDFS. It ingests unstructured text, distributes the workload, and outputs an inverted index. Demonstrates core search engine architecture, fault tolerance, and efficient parallel processing.
